# 2026-07-20 작업일지

- 상태: 대표 문진 기준점 정리와 Modal 외부 데모 설계·계획 완료
- 현재 단계: Modal 구현 Task 1 시작 전
- 기준 문서: [Modal 외부 데모 설계](../superpowers/specs/2026-07-20-modal-medgemma-external-demo-design.md)
- 진행 현황: [구현 체크리스트](../plans/2026-07-16-003-medical-interview-implementation-checklist.md)

## 결정

- 외부 사용자가 링크를 아는 경우 로그인 없이 접근하는 데모로 정했다.
- 실제 환자 정보 대신 합성 Persona 역할극만 허용한다.
- 자유 텍스트는 허용하되 실제 정보 오입력을 완전히 차단할 수 없다는 잔여 위험을 기록했다.
- MedGemma 실행 기반을 Vertex AI에서 Modal custom App으로 변경했다.
- 브라우저는 Modal을 직접 호출하지 않고 Next.js Route Handler만 인증 endpoint를 호출한다.
- T4 1개, scale-to-zero, 최대 GPU container 1개, 60초 deadline으로 시작한다.
- session 5회/분, IP 20회/시간, 전체 actual 100회/일과 server kill switch를 둔다.
- Modal Workspace budget은 월 $30 hard cap으로 설정한다.
- 음성 입력은 실제 마이크·녹음·STT 없이 합성 Persona transcript를 채우는 모의 기능으로 정했다.
- 질문 수가 상황에 따라 달라지므로 단계 번호와 고정 진행률을 표시하지 않는다.

## 문서 작업

- Modal 외부 데모 설계와 런타임·보안·검증 상세 문서를 승인 상태로 갱신했다.
- Modal 구현을 계약, Next provider, Modal runtime, 화면 연결, actual gate의 5개 task로 나눴다.
- 모의 음성 입력을 transcript registry, 상태·UI, 금지 API·E2E의 3개 task로 나눴다.
- 활성 기술 결정의 Vertex·loopback·실제 STT 가정을 Modal·공개 합성 데모·모의 음성으로 교체했다.
- README, 실행 계획, 구현 체크리스트와 진행 기록을 현재 결정에 맞췄다.
- `.env.example`의 Vertex 변수는 provider 구현 전이므로 유지하고 Modal Task 2에서 교체한다고 명시했다.

## 코드·주석 점검

- 새 코드와 기존 변경의 주석을 검색해 한글 주석 규칙을 확인했다.
- repository에서 credential과 private key 형식이 추가되지 않았는지 확인했다.
- 제공된 iPhone PNG, 9개 fixture, 문진 상태 전환과 테스트 파일을 정리 대상에 포함했다.

## 검증

| 항목 | 결과 |
|---|---|
| `npm run lint` | 통과 |
| `npm run typecheck` | 통과 |
| `npm run test:unit` | 5개 파일·31건 통과 |
| 아이콘 계약 | 6건 통과 |
| token·Pretendard 계약 | 4건 통과 |
| `npm run build` | Next.js 16.2.10 Turbopack 통과 |
| `npm run test:e2e` | webpack production build·Chromium 14건 통과 |

`test:e2e` lifecycle 안에서 Turbopack build worker가 compile 단계에 정지하는 현상을 두 번 재현했다. 일반 `npm run build`는 정상 완료했고 같은 lifecycle의 Next.js 공식 webpack build도 통과했다. 일반 build는 기본 Turbopack을 유지하고 Playwright 준비 build만 `next build --webpack`으로 변경했다.

## 미완료·다음 작업

- Modal provider, Route Handler, quota gate와 실제 배포는 아직 구현하지 않았다.
- 실제 마이크·STT와 실제 환자 데이터는 구현 범위가 아니다.
- 다음 작업은 Modal 계획 Task 1의 공유 command·DTO·validator 계약이다.
