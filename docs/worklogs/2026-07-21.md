# 2026-07-21 작업일지

- 상태: Modal 외부 데모 Task 1~5 완료·`main` 병합
- 현재 단계: 운영 kill switch 잠금, U1 잔여 gate와 U2 저장 계약 착수 대기
- 기준 문서: [Modal 외부 데모 구현 계획](../superpowers/plans/2026-07-20-modal-medgemma-external-demo-implementation-plan.md)
- 진행 현황: [구현 체크리스트](../plans/2026-07-16-003-medical-interview-implementation-checklist.md)

## 구현

- 일반 `/interview/new`는 HTTP command를, 승인된 fixture query는 결정론적 fixture command를 사용하도록 분리했다.
- `persona=kim|lee|park` allowlist와 `kim` 기본값을 적용하고 알 수 없는 값은 404로 처리했다.
- 저장된 답변의 slot·최근 history·합성 persona만 version 1 context로 question·summary Route Handler에 보낸다.
- 직접 식별정보 표지가 있으면 fetch 전에 typed error로 수정 안내를 표시한다.
- 요청마다 `AbortController`를 만들고 새 요청, reset, unmount에서 이전 요청을 폐기한다.
- transport가 abort를 무시해도 request generation이 지난 응답의 resolve를 폐기한다.
- 역할극 확인 전에는 입력과 제출을 잠그고 실제 정보 입력 금지 안내를 시작 영역과 자유 입력 가까이에 배치했다.
- question provider 실패 시 새로 저장한 turn을 포함한 history를 유지하고 수동 질문으로 계속한다.
- complete 뒤 summary를 한 번 요청하고 실패·잘못된 evidence는 같은 history의 `fallback-v1-*` 결정론적 요약으로 전환한다.
- 생성 요약은 주관적 정보·객관적 정보·확인이 필요한 정보 heading과 plain text item만 표시한다.
- 질문 단계 번호와 고정 진행률은 추가하지 않았다.

## TDD와 검증

- HTTP command 파일 부재, 역할극 잠금 부재, summary 미호출, unmount 폐기 부재로 새 테스트가 실패하는 RED를 먼저 확인했다.
- safety fixture가 duration slot을 사용하던 실패를 추가로 확인하고 safety slot으로 수정했다.
- 독립 리뷰에서 prototype key allowlist 우회, demo history·slot 불일치, abort 무시 transport의 stale resolve를 찾아 RED 테스트 뒤 수정했다.
- `npm run test:unit -- tests/unit/interview tests/unit/ai tests/unit/demo`: 10개 파일·85건 통과.
- `npm run lint`: 통과.
- `npm run typecheck`: 통과.

## 범위와 다음 작업

- 실제 환자 정보·실제 음성·마이크·STT를 사용하지 않았다.
- Modal 배포와 합성 데이터 GPU 진단을 수행했으며 실제 환자 정보는 사용하지 않았다.
- Task 4 구현 검증 시점에는 commit·push를 수행하지 않았다.
- 이후 사용자 요청에 따라 Task 1~5 변경을 커밋·푸시하고 `main`에 병합했다.
- 다음 작업은 U1의 단일 핵심 행동·Node Route 인증 gate를 닫고 U2의 IndexedDB·profile·clinician view 계약을 확정하는 것이다.

## Task 5 진행 기록

아래 항목은 준비부터 actual 검증까지의 순차 기록이며, 중간 상태는 뒤 항목에서 갱신될 수 있다.

- `tests/actual`에 세 합성 Persona의 cold 1회·warm 2회·요약과 인증·session·IP·전체 quota threshold를 검증하는 opt-in serial harness를 추가했다.
- actual 실패 메시지는 code·status만 남기고 prompt·answer·response·URL·token을 출력하지 않도록 했다.
- `RUN_MEDGEMMA_ACTUAL=1`이 없으면 5건 모두 skip하며, endpoint와 인증값은 server-only 환경 변수에서만 읽는다.
- 역할극 확인 UI를 반영해 E2E keyboard 순서와 3개 snapshot을 갱신했다.
- Next.js 16 Turbopack build가 캐시·worker 설정과 무관하게 반복 정지해 공식 webpack opt-out을 기본 production build에 적용했다.
- `npm run test:actual`: 2개 파일·5건 skip.
- `npm run lint`, `npm run typecheck`: 통과.
- `npm run test:unit`: 10개 파일·85건 통과.
- `npm run test:e2e`: Chromium 14건 통과.
- `npm run build`: webpack production build 통과.
- `.venv/bin/python -m pytest tests/modal`: 25건 통과. 시스템 `python3`에는 pytest가 없어 저장소 가상환경을 사용했다.
- Modal deploy·GPU 호출·quota smoke는 실행하지 않았고, 실제 환자 정보·실제 음성·마이크·STT도 사용하지 않았다.
- 사용자가 Hugging Face 약관, `medgemma-hf`, `medgemma-runtime=1`, proxy token, 배포·비용 발생을 승인했고 Workspace hard cap은 계획보다 낮은 `$10`으로 설정했다.
- Modal 프로필과 `main` 환경의 두 Secret 이름을 확인했으며 값은 읽거나 기록하지 않았다. 로컬 셸의 proxy token 환경 변수는 아직 미설정이다.
- `main` 배포는 저장소 소스와 런타임 구성을 외부 Modal 서비스로 업로드하는 작업에 대한 별도 명시 승인이 필요해 실행 전 안전 심사에서 차단됐다. 우회나 GPU 호출은 수행하지 않았다.
- 사용자가 소스·런타임 구성 업로드를 명시 승인한 뒤 배포를 재개했다. 계획의 파일 경로 명령은 상대 import 때문에 실패해 Modal 안내의 module mode로 교정했다.
- 첫 이미지 build에서 `transformers==5.14.1`이 요구하는 `huggingface-hub>=1.5.0`과 기존 `1.3.4` pin 충돌을 확인했다. 계약 테스트 RED 뒤 `1.5.0`으로 최소 수정했고 Python 25건을 재검증했다.
- Modal `main` 배포와 gated model 이미지 다운로드가 성공했다. 무인증 POST는 response 본문 없이 401을 확인했으며 actual kill switch는 계속 `1`이다.
- 상위 저장소의 gitignored `.env.local`에서 endpoint·proxy ID·proxy secret의 존재만 확인했다. 인증 요청은 503 `actual-disabled`에서 GPU 전에 거절됐다.
- actual을 잠시 활성화한 첫 실행은 세 Persona 모두 65초 idle 뒤 60초 timeout으로 실패했다. system/stderr에서 MedGemma processor에 필요한 torchvision·Pillow 누락을 확인했다.
- PyPI 계약에 맞는 `torchvision==0.28.0`, `pillow==12.3.0`과 `huggingface-hub==1.5.0`을 TDD 추가했고 이미지·모델 weight 로딩은 성공했다.
- 생성 상한을 질문 96·요약 384로 축소했으나 T4 float16 actual은 입력 192개를 정상 인식한 뒤 출력 96개를 모두 PAD로 생성했다. prompt·answer·response 본문은 출력하지 않고 token 수만 계측했다.
- 비용 절감을 우선해 L4 전환 대신 T4 첫 토큰 forward만 두 차례 진단했다. 기본 backend와 eager attention 모두 vocabulary logits 262,208개가 전부 NaN이어서 PAD는 원인이 아니라 NaN argmax 결과임을 확인했다.
- eager attention에서도 동일해 attention backend·생성 반복·prompt 문제를 배제했다. 직접 원인은 T4 float16 forward의 수치 붕괴이며 정확히 처음 NaN이 생기는 layer·연산은 아직 특정하지 않았다. 진단 코드는 제거하고 actual kill switch를 `1`로 복구해 재배포했다.
- 비용 우선 대안으로 `bitsandbytes==0.49.2` 8비트 선형 가중치와 float32 잔여 연산을 TDD 적용했다. 첫 logits 262,208개는 전부 유한했고 메모리는 약 6.34GB였으며 8토큰 생성도 PAD 없이 성공했다.
- 최종 cold 질문은 약 69.9초에 web 65초 timeout으로 500이 됐고, GPU는 취소 뒤 96토큰을 PAD 없이 생성했다. 이어진 warm 질문은 24.7초 HTTP 200이었지만 96토큰 안에 JSON을 완성하지 못했다.
- 8비트는 NaN을 해결했지만 cold ≤60초, warm ≤15초, 구조화 응답 gate를 충족하지 못했다. 4비트와 추가 토큰 호출은 실행하지 않았고 진단 코드를 제거한 뒤 actual kill switch를 `1`로 복구했다.
- 질문 prompt에 단일 compact JSON, 앞뒤 설명·Markdown·코드펜스 금지, 고정 field, 단일 짧은 option을 TDD 추가했다. Python 27건과 기존 TypeScript 단위 85건·lint·typecheck를 통과했다.
- 모든 slot이 채워진 합성 cold seed 1회 뒤 측정한 compact prompt warm 질문은 26.5초였고, 96토큰·EOS 0으로 내부 JSON이 완성되지 않았다. 응답 본문은 출력하거나 기록하지 않았다.
- warm ≤15초·schema gate가 실패해 cold 최적화 검토의 선행 조건을 충족하지 못했다. 추가 GPU 호출, 4비트, 전체 actual suite를 중단하고 actual kill switch를 `1`로 복구했다.
- kill switch Secret을 `1`로 갱신한 직후 기존 web 컨테이너가 이전 `0` 값을 사용해 첫 확인이 200이었다. 즉시 같은 소스를 재배포해 컨테이너를 교체했고 최종 503 `actual-disabled`를 확인했다. 이후 Secret 전환은 재배포와 503 확인까지 한 절차로 취급한다.
- 전체 provider JSON 예시를 제거하고 assistant prefill로 slot·text만 생성한 뒤 런타임이 고정 필드를 조립하도록 TDD 구현했다. 질문 상한은 64토큰이며 모든 slot이 채워지면 생성 없이 complete를 반환한다.
- Python 34건·기존 TypeScript 단위 85건·lint·typecheck 통과 후 생성 없는 cold seed는 53.9초, warm 질문은 9.0초·유효 JSON으로 통과했다. 새 배포의 실제 cold 질문은 유효 JSON이지만 67.2초로 cold ≤60초를 실패했다.
- alpha GPU memory snapshot은 최초 seed가 web timeout이 되고 snapshot 컨테이너가 생성 완료 없이 Pending 상태에 머물렀다. 해당 GPU 컨테이너만 종료하고 snapshot 설정을 제거했다.
- prefill·8비트 런타임을 재배포하고 kill switch `1`, 인증 503 `actual-disabled`를 최종 확인했다.
- 상시 GPU 비용을 피하기 위해 T4 `min_containers=0`, `max_containers=1`, 60초 scale-down은 유지하고 cold 기준을 75초로 조정했다. CPU web timeout은 85초, Next provider 기본 timeout은 75초·상한은 85초로 분리했다.
- 합성 `kim` persona만 사용한 비용 제한 검증에서 cold 69.378초, warm 5.518초·5.223초였고 세 응답 모두 구조 검증을 통과했다. 응답 본문은 출력하거나 기록하지 않았다.
- 위 결과는 latency 부분 gate이며 세 Persona 질문·요약과 quota·fallback 전체 gate는 아직 실행하지 않았다. 검증 뒤 Secret을 `1`로 복구하고 재배포해 인증 503 `actual-disabled`를 확인했다.
- 비용 우선 기준을 처음 조정한 시점에는 lint·typecheck, TypeScript 단위 10개 파일·86건, Modal Python 34건, `git diff --check`가 통과했고 당시 opt-in actual 7건은 비활성 상태에서 명시적으로 skip됐다.
- 최초 전체 actual은 질문 뒤 384토큰 요약이 GPU 60초 timeout과 retry를 반복해 실패했다. 요약을 assistant prefill 기반 64토큰 text 생성과 runtime evidence 조립으로 TDD 수정했다.
- actual harness는 실행별 비식별 session/IP hash와 단일 fail-fast 순차 테스트를 사용하도록 고쳤다. 65초 idle에서 종료 직전 컨테이너를 사용한 다음 요청이 cold start가 되는 경계를 확인해 idle을 90초로 조정했다.
- 최종 actual에서 세 합성 Persona의 질문 9회·요약 3회가 cold ≤75초, warm·요약 ≤15초와 schema·근거 gate를 통과했다. 실제 환자 정보·실제 음성·마이크·STT는 사용하지 않았고 응답 본문도 기록하지 않았다.
- 새 Modal `test` 환경의 GPU 없는 quota smoke 앱에서 무인증 401, session 6번째·IP 21번째·전체 101번째 429를 4/4로 확인한 뒤 앱을 중지했다. adapter·fallback 관련 단위 36건도 통과했다.
- 운영 kill switch는 Secret `1` 갱신과 재배포를 마쳤고 인증 요청의 503 `actual-disabled`를 최종 확인했다.
- 최종 검증에서 `git diff --check`, lint, typecheck, TypeScript 단위 86건, Modal Python 36건, Chromium E2E 14건, webpack production build가 통과했다. E2E 첫 시도는 sandbox loopback bind `EPERM`이었고 동일 명령을 로컬 서버 권한으로 재실행해 통과했다.
- Modal 상태 확인에서 main/test 실행 task·container는 모두 0개였고 quota smoke 앱은 stopped였다. 이 검증 시점에는 commit·push를 수행하지 않았다.

## 저장소 통합 상태

- `ac5be27`에 Modal MedGemma 계약·Next proxy·runtime 기반을 커밋했다.
- `d9bd843`에 Modal Task 1~3 인계 기록을 커밋했다.
- `9d04c54`에 화면 연결·역할극 확인·stale 폐기·history 보존 fallback을 커밋했다.
- `5940fd2`에 비용 우선 Modal runtime 보정·actual gate·문서 검증을 커밋했다.
- `codex/modal-contracts`를 원격에 푸시한 뒤 `main`에 fast-forward 병합했다.
- 최종 확인에서 `main`과 `origin/main`의 divergence는 `0 0`이었다.
- 병합된 `main`에서 lint·typecheck·단위 86건·Modal Python 36건·Chromium E2E 14건·webpack production build가 통과했다.
