> [문서 인덱스](../2026-07-10-medical-interview-pwa-design.md)
> 이전: [15. 저장 방식](./004-15-저장-방식.md)
> 다음: 없음


- Figma 기준 주요 화면이 있다.
- 아래쪽 탭, 헤더, 버튼, 입력창이 Figma와 비슷하다.
- 기본 프로필을 입력하면 저장된다.
- 키와 몸무게는 의료정보로 저장된다.
- 저장된 정보는 다음 문진에서 다시 사용된다.
- 글 입력으로 문진을 끝낼 수 있다.
- 음성 입력 결과를 고칠 수 있다.
- 음성 입력 중 앱이 듣고 있다는 표시가 보인다.
- 3.5초 이상 말소리가 없으면 말하기 종료로 처리된다.
- 사진을 첨부할 수 있다.
- 사진은 저장 전에 최대 1024px 정도로 압축된다.
- 영상은 첨부 목록에만 보인다.
- AI 실패 시 다시 시도할 수 있다.
- AI 실패 시 수동 요약을 볼 수 있다.
- 의료진용 S/O 요약이 만들어진다.

### 안전 테스트

- AI가 진단명을 만들지 않는다.
- AI가 치료 방법을 추천하지 않는다.
- AI가 약 복용을 지시하지 않는다.
- 응급 신호는 진단이 아니라 안전 안내만 보여준다.
- 이름과 연락처는 AI 요청에 포함되지 않는다.

### 접근성 테스트

- 모바일 화면에서 버튼 크기가 최소 48px 이상이다.
- 본문 글자 크기가 최소 16px 이상이다.
- 글자를 키워도 내용이 잘리지 않는다.
- 색깔 없이도 상태를 이해할 수 있다.
- 화면 읽기 순서가 자연스럽다.

### 디자인 비교 테스트

- Figma `최종 prototype`과 실제 화면을 비교한다.
- 393px 모바일 폭에서 Figma와 비슷하게 보인다.
- `Buttons`, `Chat_Input`, `speech_bubble`, `Summary_Item`, `FormInput`, `GNB`, `Header` 역할을 따른다.
- Figma에 없는 설명 화면을 마음대로 많이 추가하지 않는다.

## 21. 구현 우선순위

1. Figma 화면 구조 확인
2. Next.js 프로젝트 만들기
3. PWA 기본 설정
4. 필수 화면을 Next.js 라우트에 연결
5. IndexedDB 저장 구조 만들기
6. 기본 프로필 저장 연결
7. 의료정보 저장 연결
8. 문진 입력 연결
9. 저장 후보 확인 UI 연결
10. Hugging Face 서버 API 만들기
11. MedGemma 질문 프롬프트 만들기
12. MedGemma 요약 프롬프트 만들기
13. 사진 첨부 연결
14. 영상 첨부 저장
15. 기록 목록 연결
16. 의료진용 요약 화면 연결
17. 응급 안전 안내 연결
18. 접근성 확인
19. Figma와 화면 비교

## 22. 기술 결정

확정된 것:

- GitHub 저장소: [divclasssg/KODDI_UD_Eum](https://github.com/divclasssg/KODDI_UD_Eum.git)
- 웹 프레임워크: Next.js
- 라우팅 방식: Next.js App Router
- 서버 API: Next.js Route Handlers
- UI 제작: 사용자가 Figma 기준으로 직접 제작
- 저장 방식: IndexedDB
- AI 연결: Next.js 서버 API에서 Hugging Face 호출
- Hugging Face 호출 방식: Next.js 서버에서 Hugging Face Inference Providers OpenAI 호환 Chat Completions API 직접 호출
- 잠금 설정 방식: 1차 데모에서는 4자리 PIN
- 배포 전 데모: 로컬 실행 우선
- 배포 위치: Vercel

아직 정해야 하는 것:

- Figma에 없는 저장 확인 화면의 최종 디자인
- Figma에 없는 설정 화면의 최종 디자인
