> [상위 문서](../2026-07-16-003-medical-interview-implementation-checklist.md)
> 이전: [현재 상태와 구현 전 결정](./01-status-and-decisions.md)
> 다음: [Day 2 · U2/U3](./03-day-2-u2-u3.md)
## Day 1 · U1 Project Foundation

### 프로젝트 설정

- [x] Next.js App Router 프로젝트 생성
- [x] TypeScript 활성화
- [x] ESLint 설정
- [x] React Compiler 활성화 또는 production build 실패 시 제외 결정 기록
- [x] `src/` 구조 사용
- [x] `@/*` import alias 설정
- [x] package manager와 Node.js 버전 고정
- [x] `.gitignore`에 `.env.local`, credential, build output 포함
- [x] `.env.example`에 secret 없는 MedGemma 변수 이름만 기록
- [x] Vitest·Testing Library jsdom 단위 테스트 기반 구성
- [x] Playwright Chromium production server E2E 실행 계약 구성

### SCSS와 디자인 토큰

- [x] `sass` 직접 의존성 설치·SCSS 전역 연결
- [x] 애플리케이션 스타일은 SCSS만 사용하고 `postcss`·`autoprefixer` 직접 의존성 및 별도 설정은 추가하지 않기로 확정
  - Next.js 16.2.10의 기본 `postcss-preset-env` Autoprefixer가 브라우저 접두사를 처리하고, 전이 `postcss@8.4.31`은 프레임워크 내부 의존성으로만 유지
  - 커스텀 `postcss.config.*`를 추가하면 Next.js 기본 플러그인이 대체되므로 현재는 만들지 않음
- [x] primitive token 작성
- [x] semantic token 작성
- [x] component token 계층과 생성 기준 작성
  - 승인 기준인 소비자 2개 이상 또는 상태 교체 요구를 충족한 컴포넌트가 없어 alias는 아직 생성하지 않음
- [x] `:root` CSS Custom Properties 생성
- [x] Pretendard Variable v1.3.9 폰트·라이선스 고정과 `next/font/local` 자체 호스팅
- [x] 단일 라이트 모드 적용과 기존 다크 모드 분기 제거
- [x] class name을 하이픈 스타일로 유지하는 convention 기록
- [x] Figma URL 수신으로 fallback token 단계 생략
- [x] Figma variable/node → token 매핑 명세 승인
  - 증거: [Design Token Foundation](../../superpowers/specs/2026-07-16-design-token-foundation-design.md)

### 대표 문진 화면

- [x] [질문 카드·입력·동작 상태·주요 행동 구현 명세 승인](../../superpowers/specs/2026-07-19-interview-screen-design.md)
- [x] [기본·비동기·오류·안전·요약 fixture 설계 승인](../../superpowers/specs/2026-07-19-interview-state-fixture-design.md)
- [x] 문진 상태 모델·9개 fixture registry·서버 allowlist resolver 구현
- [x] 393×852 고정 viewport·iPhone PNG 프레임·iOS 상태바·문진 헤더 구현
- [x] 전체 대화 log·현재 질문·선택·텍스트·음성 입력·명시적 `다음` 구현
- [x] `/interview/new` route 생성
- [x] 393px 질문 카드·입력·진행 상태·주요 행동 구현
- [x] 기본·저장 중·AI 대기·오류·안전 안내·요약 전환 fixture 구현
- [x] 본문 18px 이상
- [x] 주요 조작 영역 48px 이상
- [x] visible focus, non-color state, live status 적용
- [x] 한 화면에 질문 하나와 핵심 행동 하나만 노출

### Day 1 MedGemma·환경 Gate

- [x] 대상 desktop Chromium과 393px viewport 고정
- [x] 개발·production server를 `127.0.0.1`에 bind
- [x] Modal model·GPU·scale-to-zero·endpoint·quota·budget 계약 승인
- [x] version 1 문진 context·질문·요약 DTO와 exact-object validator 구현
- [x] 8,192 byte·최근 turn 10개·summary 근거 ID 검증 구현
- [x] 전화번호·이메일·주민등록번호·주소/기관 표지 직접 식별정보 탐지 구현
- [x] UI와 adapter가 공유하는 persona·slot·command port 계약 구현
- [x] session 분당 5회·IP 시간당 20회·actual 일일 100회 quota 함수 구현
- [x] Modal CPU gate 직렬화·T4 scale-to-zero GPU runtime 코드 구현
- [x] `.env.example`을 `mock|modal` target contract로 교체
- [x] Modal Secret·proxy token·Workspace budget 실제 설정
- [x] Node.js Route Handler에서 인증 성공
- [x] 브라우저→Node Route→Modal 질문 HTTP 200과 새 질문 표시
- [x] 실제 한국어 질문 1회 응답
- [x] JSON 추출·schema 검증·timeout/error mapping 성공
- [x] 실제 payload를 로그에 남기지 않음 확인

### U1 Exit Evidence

- [x] lint 통과
- [x] typecheck 통과
- [x] production build 통과
- [x] unit tests 통과
- [x] 393px layout E2E 통과
- [x] 대표 화면 캡처 또는 브라우저 검수 기록

증거(2026-07-16): Next.js 16.2.10·React 19.2.4 scaffold, Node 22.17.0·npm 11.18.0 고정, `sass@1.101.0` 직접 설치와 `.scss` 연결, `.env.example` 추적 가능·credential JSON ignore 확인, 개발·production 서버 `127.0.0.1:3000` HTTP 200 확인, `npm run lint`, `npm run typecheck`, `npm run build` 종료 코드 0.

증거(2026-07-17): `src/styles/tokens/`의 primitive·semantic·typography·component 계층과 `src/styles/_tokens.scss` entrypoint, 전체 승인값 Sass 출력 비교, consumer primitive 차단, 다크 모드 부재, 상태 대비, Pretendard SHA-256·fallback 검증 4건 통과. `next/font/local` production CSS에 `font-display: swap`, `font-weight: 45 920`, 로컬 WOFF2 번들 확인. `npm run lint`, `npm run typecheck`, `npm run build` 종료 코드 0. 대표 문진 화면·393px E2E는 미완료.

증거(2026-07-19): Vitest 4.1.10, jsdom 29.1.1, Testing Library, Playwright 1.61.1과 로컬 Chromium 설치. `test:unit`·`test:e2e` script, jsdom setup, production server E2E 설정을 추가했다. 기반 단위 테스트 1건과 `npm run lint`, `npm run typecheck`가 통과했다.

증거(2026-07-19): 문진 화면 상태 타입, 합성 데이터만 사용하는 9개 fixture registry, 서버 flag와 ID allowlist resolver를 추가했다. ID·접근성 기대값·5개 turn·금지 필드 계약을 포함한 단위 테스트 14건과 기존 기반 테스트 1건, lint·typecheck가 통과했다.

증거(2026-07-19): 승인 SHA-256과 일치하는 1350×2760 iPhone PNG를 정적 자산으로 복사하고, 440×900 셸 안에 393×852 viewport, 48px iOS 상태바, 62px 문진 헤더를 구현했다. 장식 접근성·홈 링크 단위 테스트, 디자인 토큰 계약 4건, lint·typecheck가 통과했다. 실제 화면 정렬과 200% 확대 검수는 대표 route 완성 뒤 수행한다.

증거(2026-07-19): 전체 확정 대화 log와 현재 질문, native radio·checkbox, 자유 입력·음성 입력 버튼, 명시적 `다음`을 구현했다. 선택·수정 텍스트 보존과 같은 질문의 중복 제출 차단을 포함한 문진 단위 테스트 18건, 토큰 4건, 아이콘 6건, lint·typecheck가 통과했다. 브라우저 시각·크기 검수는 route 완성 뒤 수행한다.

증거(2026-07-19): `/interview/new` Server Component에서 서버 전용 `INTERVIEW_FIXTURE_MODE`와 allowlist로 fixture query를 제한하고, 고정 프레임 안에 상태 controller를 연결했다. 저장 300ms 표시 임계값, 900ms 저장, 저장 완료 후 1,200ms AI 호출, 초안 보존 재시도, AI만 재시도, 긴급 행동 후 안전 종료를 구현했다. 직접 상태 9개와 복구 전환을 포함한 문진 단위 테스트 31건, 아이콘 계약 6건, 토큰 계약 4건, lint·typecheck·production build가 통과했다. fixture adapter는 실제 IndexedDB·MedGemma·전화·공유 성공 증거가 아니다.

증거(2026-07-19): Playwright Chromium 1280×1000 테스트 창에서 393×852 앱 viewport와 440×900 프레임을 측정하고 9개 fixture 직접 진입, 긴 대화 최신 질문 복귀, Tab·Space·Enter 답변 제출과 AI 오류 재시도, 프레임 내부 focus를 검증했다. 전체 기기 1개와 대표 앱 viewport 3개의 snapshot을 승인한 뒤 E2E 14건을 변경 없이 재실행해 통과했다. 실제 Chrome 200% 확대는 자동 적용 대신 사용자 수동 확인으로 전환했다.

증거(2026-07-19): 사용자가 실제 Chrome 200% 확대에서 바깥 문서와 내부 대화 scroll, 질문·모든 입력·주요 행동 접근, visible focus를 수동 확인했다. CSS `zoom`이나 CDP page scale은 증거로 사용하지 않았다.

증거(2026-07-20): 공개·익명 외부 데모를 합성 Persona와 Modal MedGemma로 제공하고, 실제 마이크·녹음·STT 대신 결정론적 모의 음성 입력을 사용하기로 승인했다. provider·보안·quota·비용·actual gate 계획을 작성했으며 구현 완료로 표시하지 않았다.

증거(2026-07-20): Modal Task 1에서 persona·slot·공유 command port, version 1 AI context·질문·요약 계약, unknown field를 거절하는 exact-object validator와 직접 식별정보 표지 탐지기를 구현했다. 신규 계약·탐지 테스트가 모듈 부재로 실패하는 것을 먼저 확인했고, AI·demo·interview 단위 테스트 7개 파일·49건과 typecheck가 통과했다. Modal provider·Route Handler·실제 endpoint 호출은 아직 구현하지 않았다.

증거(2026-07-20): Modal Task 2에서 server-only mock·Modal adapter, 60초 timeout, 선택적 1회 재시도, proxy 인증 header, Origin·JSON·크기·식별정보 guard, 익명 HttpOnly cookie와 session/IP HMAC, question·summary Route Handler를 구현했다. `.env.example`의 Vertex 변수를 제거하고 `mock|modal` 계약으로 교체했다. 실제 Modal 인증과 GPU 호출은 아직 검증하지 않았다.

증거(2026-07-20): Modal Task 3에서 Pydantic exact schema, UTC quota bucket, prompt의 진단·치료 금지와 untrusted context 구분, `max_containers=1` CPU gate, T4 scale-to-zero GPU class, 인증 quota smoke app을 구현했다. Python 테스트 25건이 통과했지만 Modal deploy·Secret·실제 GPU 기동은 수행하지 않았다.

증거(2026-07-21): Modal Task 4에서 일반 `/interview/new`를 HTTP adapter에 연결하고 fixture query는 기존 결정론적 adapter로 분리했다. `kim|lee|park` persona allowlist, 역할극 확인 전 입력·요청 잠금, 직접 식별정보의 fetch 전 수정 안내, 요청 generation 기반 stale 폐기, history 보존 수동 질문과 version 1 결정론적 요약 fallback을 구현했다. 관련 단위 테스트 10개 파일·85건, lint, typecheck가 통과했으며 실제 Modal 호출은 수행하지 않았다.

증거(2026-07-21): 사용자가 Hugging Face 약관, `medgemma-hf`와 `medgemma-runtime` Secret, Modal proxy token, Workspace `$10` hard cap 설정을 완료했다. 값은 읽거나 문서·git에 기록하지 않았다.

증거(2026-07-21): 세 합성 Persona의 실제 한국어 질문 9회·요약 3회가 구조·근거·cold/warm gate를 통과했다. payload 본문 없이 상태·token 수·latency만 확인했으며 timeout/error mapping 단위 테스트도 통과했다. 브라우저를 통한 Node Route Handler 인증 성공은 별도 항목으로 남긴다.

증거(2026-07-22): 상태 fixture별 `[data-action-emphasis]` 계약을 TDD로 추가했다. 행동 가능 상태는 primary CTA가 정확히 하나이고 대기·완료 상태는 0개이며, AI 오류·긴급 안내의 보조 행동은 secondary다. primary 전용 단위 13건과 관련 문진 단위 34건이 통과했다.

증거(2026-07-22): credential 없는 브라우저 Route gate는 mock에서 HTTP 200과 새 질문을 확인했다. Modal `main` actual 1회는 Node guard와 proxy 인증을 통과해 T4가 cold load 후 합성 질문 17토큰을 생성했지만, 응답이 Next provider 75초 안에 반환되지 않아 `/api/ai/question`이 HTTP 502 fallback으로 종료됐다. 추가 GPU 요청 없이 kill switch `1` 재배포, 인증 status-only 503, 실행 container 0을 확인했다. 따라서 인증 항목은 완료하되 end-to-end HTTP 200 항목과 U1 unit 완료는 보류한다.

증거(2026-07-22): direct actual의 cold 성능 기준과 운영 provider 기본 75초는 바꾸지 않고, Next·네트워크 왕복을 포함하는 Playwright actual server만 기존 provider 상한 85초를 사용하도록 실패 테스트부터 보정했다. config 계약 단위 1건은 RED 후 GREEN을 확인했으며 실제 GPU 재검증은 수행하지 않았다.

증거(2026-07-22): 사용자 승인 뒤 합성 `kim` Persona 질문 1회만 browser actual로 재검증했다. `/api/ai/question` HTTP 200과 새 질문 표시가 58.7초에 통과했다. 즉시 kill switch `1`을 재배포하고 인증된 status-only 합성 요청의 HTTP 503과 실행 container 0을 확인했다. 실제 환자 정보·실제 음성·마이크·STT와 응답 본문·credential 기록은 사용하지 않았다.
